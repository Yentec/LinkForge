# Application
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
BASE_URL="http://localhost:3000"
LOG_LEVEL=info

# Database (matches docker-compose.yml)
DATABASE_URL="postgresql://linkforge:linkforge@localhost:5432/linkforge?schema=public"

# Redis (matches docker-compose.yml)
REDIS_URL="redis://localhost:6379"

# Secrets — replace before running. JWT secrets >= 32 chars, IP salt >= 16 chars.
JWT_SECRET="dev-only-change-me-to-a-32-char-minimum-secret"
JWT_REFRESH_SECRET="dev-only-change-me-different-32-char-min-secret"
IP_HASH_SALT="dev-only-16-chars-min"